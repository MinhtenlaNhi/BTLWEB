mixin box-menu(data)
    ul
        each item in data
            li
                a(href=`/products/${item.slug}` data-id=item.id) #{item.title}
                if(item.children && item.children.length > 0)
                    +box-menu(item.children)